class action:

    def __init__(self, e, w1, w2, a):
        self.event = e   #(int)constant de const.py referida al event
        self.widget_1 = w1  #(int)element del vector selecionat
        self.widget_2 = w2 #(int)element del vector selecionat 
        self.array = a   #(array int) vector reordenat actualitzat
        